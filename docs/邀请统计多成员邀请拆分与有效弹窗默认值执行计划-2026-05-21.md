# WeFlow-main 待确认默认值与多成员邀请拆分计划

## Summary

- 在 `WeFlow-main` 里完成两项小范围改动：待确认记录点“有效”时，弹窗里的“被邀请人微信 ID”默认填入被邀请人昵称；多成员邀请系统消息按成员拆成多条邀请事件。
- 本次默认只处理新扫描进入的数据，不自动改写本地已扫描历史记录。
- 执行阶段先把本计划保存到 `D:\work\temp\WeFlow\docs\邀请统计多成员邀请拆分与有效弹窗默认值执行计划-2026-05-21.md`，全部完成后再保存变更记录到同目录。

## Key Changes

- 前端弹窗：在 `src/pages/InviteStatsPage.tsx` 的 `openManualInviteDialog` 中调整 `confirm` 模式初始化逻辑，把 `wxId` 默认值从“仅使用 `row.wx_id`”改为 `row.wx_id || row.user`；输入框继续保持可编辑。
- 多成员邀请解析：在 `electron/services/inviteStatsService.ts` 中加固邀请消息解析，确保 `"A"邀请"B、C、D、E"加入了群聊`、中英文逗号/顿号分隔、外层引号等格式都会返回多条 `ParsedSystemEvent`。
- 事件处理复用现有单条邀请链路：每个拆分事件继续走 `buildInviteEvent`、`hasInviteEvent`、`upsertRawEventFromInvite`、`buildMemberTraceRows`，不新增表结构、不改 IPC 接口、不改远程同步字段。
- 拆分后的多条记录保留同一条 `raw_content`、`parsed_content`、`source_message_id`、`source_local_id`、`source_create_time`；去重键继续依赖“源消息 + 群 + 被邀请人”，避免同一原始消息里的 B/C/D/E 互相覆盖。

## Test Plan

- 静态验证：执行 `npm run typecheck`，确认 TypeScript 通过。
- 手动场景 1：待确认记录页点击“有效”，确认“被邀请人微信 ID”输入框默认显示被邀请人昵称，且可手动修改后保存。
- 手动场景 2：用包含 `"A"邀请"B、C、D、E"加入了群聊` 的新扫描消息验证群成员溯源列表出现 4 条记录：A 邀请 B、A 邀请 C、A 邀请 D、A 邀请 E。
- 手动场景 3：确认 4 条记录的原始消息均为同一条完整系统消息，统计、待确认、有效/无效处理仍按原单条邀请规则运行。

## Assumptions

- 只改 `WeFlow-main`，不改 `WeFlow-Web` 和 Supabase 表结构。
- 不做历史数据自动补拆；已有本地旧记录如需修复，后续通过重新扫描/清理数据另行处理。
- 多成员邀请拆分后仍使用现有 `sourceRule: 'invite'`，避免引入新的下游分支。
