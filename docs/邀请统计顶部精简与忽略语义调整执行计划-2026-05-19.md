# 邀请统计顶部精简与忽略语义调整执行计划

## 目标

- 清理 `WeFlow-main` 和 `WeFlow-Web` 邀请统计页顶部左侧品牌区，只保留导航和右侧操作位。
- 主应用中，待确认记录点击“忽略”后仍保留在群成员溯源中，展示为“已忽略 / 无效”。
- 保持 `确认保存`、数据大屏统计、同步逻辑和 Supabase 表结构不变。

## 执行步骤

1. 调整 `WeFlow-main` 邀请统计页顶部结构与样式。
   - verify: 页面 JSX 不再渲染品牌图标、主标题和副标题；导航仍可用。
2. 调整 `WeFlow-main` 群成员溯源忽略记录展示与导出文案。
   - verify: `status=ignored` 页面状态显示“已忽略”，归因显示“无效”；导出保持一致。
3. 调整 `WeFlow-Web` 远程用户页顶部结构与样式。
   - verify: 顶部不再渲染品牌文案，双导航仍居中显示。
4. 运行类型检查。
   - verify: `WeFlow-main npm run typecheck` 和 `WeFlow-Web npm run typecheck` 通过。
5. 补充实施记录。
   - verify: 本文件记录实际修改、验证结果和残余风险。

## 口径确认

- 忽略记录不参与数据大屏、邀请排行榜和人数统计。
- 忽略记录不伪装成真实退群，群成员溯源状态使用“已忽略”。
- 忽略记录在群成员溯源归因展示为“无效”，用于说明该记录不参与有效归因。
- `WeFlow-Web` 保持只读用户版现有数据口径，不额外展示 `ignored` 记录。

## 实施记录

### 已修改文件

- `WeFlow-main/src/pages/InviteStatsPage.tsx`
  - 移除顶部左侧品牌块。
  - 群成员溯源中 `status=ignored` 显示为“已忽略”，归因显示为“无效”。
- `WeFlow-main/src/pages/InviteStatsPage.scss`
  - 顶部布局从“品牌区 + 导航 + 设置”调整为“导航 + 设置”。
  - 删除已不再使用的品牌区样式。
- `WeFlow-main/electron/services/inviteStatsService.ts`
  - 群成员溯源导出中 `status=ignored` 输出“已忽略 / 无效”。
  - 归因筛选“无效”包含已忽略记录。
  - 数据大屏实时动态排除已忽略记录，避免忽略项出现在大屏展示中。
- `WeFlow-Web/app/page.tsx`
  - 移除远程用户页顶部左侧品牌文案。
- `WeFlow-Web/app/globals.css`
  - 顶部布局改为导航居中展示。
  - 删除已不再使用的品牌区和占位区样式。

### 验证结果

- `D:\work\WeFlow\WeFlow-main` 执行 `npm run typecheck`：通过。
- `D:\work\WeFlow\WeFlow-Web` 执行 `npm run typecheck`：通过。

### 残余风险

- 未启动本地 Electron 页面或远程 Web 页面做截图级视觉核对。
- 未连接真实微信数据验证“确认保存”后的实际统计刷新；本次沿用现有确认保存链路，仅调整忽略记录的展示与导出口径。
